<?xml version="1.0" encoding="utf-8"?>
<!--
  packageNames is intentionally scoped to only the driver apps that have a working parser.
  Adding a new aggregator (see core/parser/OrderParser.kt) also means adding its package name
  here — this is a manifest-level filter, not application code, so it doesn't count against
  the "no existing code touched" goal for new parsers.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:packageNames="ee.mtakso.driver,com.ubercab.driver" />
